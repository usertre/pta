#include<stdio.h>
int main()
{
	double a;
	scanf("%lf",&a);
	if(a<0) printf("Invalid Value!");
	else if(a>=0&&a<=50) printf("cost = %0.2lf",0.53*a);
	else if(a>50) printf("cost = %0.2lf",50*0.53+(a-50)*0.58);
	return 0;
}
