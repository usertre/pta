#include<stdio.h>
int main()
{
	int N;
	scanf("%d",&N);
	double sum=0;int k=0;
	for(int i=1;;i+=3)
	  {
	    short b;
	    if(i%2)b=1;
		else b=-1;
		sum+=(b/(i*1.0));
		k++;
		if(k==N) break;
	  }
	printf("sum = %.3lf",sum);
	return 0;
}
