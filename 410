#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	double sum=0;
	for(int i=1;i<=n;++i)
		{
			if(i%2) sum+=(i/((2*i-1)*1.0));
			else sum-=(i/((2*i-1)*1.0));
		}
	printf("%.3lf",sum);
	return 0;
}
