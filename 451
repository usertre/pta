#include<stdio.h>
int main()
{
  int n;char p='A';
  scanf("%d",&n);
  for(int i=1;i<n;++i)
    {
      for(int j=n-i;j>=0;--j)
        printf("%c ",p++);
      printf("\n");
    }
  printf("%c ",p);
  return 0;
}
