#include<stdio.h>
int main()
{
	int m,n,t,c=0;
	scanf("%d%d",&m,&n);
	for(int i=0;i<m;++i)
	{
		scanf("%d",&t);
		if(t==n) if(i==0) c=-1; else c=i;
	}
	if(c==-1) printf("0");
	else if(c>0) printf("%d",c);
	else printf("Not Found");
	return 0;
}
