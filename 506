#include<stdio.h>
char a[4];int b[4];
int main()
{
	scanf("%s",a);
	for(int i=0;i<4;++i) {b[i]=a[i]-'0';b[i]+=9;b[i]%=10;}
	printf("The encrypted number is %d%d%d%d",b[2],b[3],b[0],b[1]);
	return 0;
}
