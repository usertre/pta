#include<stdio.h>
int t[6][6];
int main()
{
	int m,n;
	scanf("%d%d",&m,&n);
	for(int i=0;i<m;++i)
	  for(int j=0;j<n;++j)
	    scanf("%d",&t[i][j]);
	for(int i=0;i<m;++i)
	{
		int sum=0;
		for(int j=0;j<n;++j)
		  sum+=t[i][j];
		printf("%d\n",sum);
	}
	return 0;
}
